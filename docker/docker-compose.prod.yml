version: '3.8'

services:
    db:
        volumes:
          - ./db/prod/postgis-data:/var/lib/postgresql

    web:
        volumes:
          - ./db/prod/data:/data

        ports:
            - 3050:3050

        environment:
            - DJANGO_ENV=production
